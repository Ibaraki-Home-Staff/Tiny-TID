//! Route handlers placeholder — filled in the worker implementation step.

use tid_core::view::{PassSetting, ViewInput, ViewScope};
use tid_core::network::MergedScopeSource;

/// Temporary compile anchor; replaced by real routes.
pub fn _anchor(_s: &MergedScopeSource<'_>, _i: &ViewInput<'_>, _p: PassSetting) {}
